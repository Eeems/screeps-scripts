{
  "name": "eeems'screeps",
  "scripts": {
    "postinstall": "typings install",
    "prebuild": "rimraf dist/*",
    "build": "tsc && tsc --outFile dist/kernel.js -p asconfig.json && asc -o dist/kernel.wasm -v -O src/kernel/init.ts",
    "postbuild": "grunt"
  },
  "devDependencies": {
    "assemblyscript": "^0.3.0",
    "rimraf": "^2.6.1",
    "tslint": "^5.4.3",
    "typings": "^2.1.1"
  },
  "dependencies": {
    "@types/long": "^3.0.32",
    "@types/node": "^8.0.53",
    "@types/webassembly-js-api": "0.0.1",
    "assemblyscript-loader": "^0.3.0",
    "grunt": "^1.0.1",
    "grunt-contrib-copy": "^1.0.0",
    "grunt-screeps": "^1.3.0",
    "long": "^3.2.0",
    "typed-screeps": "^1.0.4",
    "typescript": "^2.6.2"
  }
}
